// Class representing a Truck with an ID and capacity
class Truck {
    int capacity; // Capacity of the truck
    int id;       // ID of the truck
    public Truck(int id, int capacity) { //Constructor
        this.id = id;
        this.capacity = capacity;
    }
}
